document.addEventListener("DOMContentLoaded",()=>{if(window.location.hash==="#ContactForm"){const form=document.getElementById("Form");if(form){const headerHeight=document.querySelector("header")?.offsetHeight||0;window.scrollTo({top:quickForm.offsetTop-headerHeight,behavior:"smooth"});}}});document.addEventListener("DOMContentLoaded",()=>{const loadingScreen=document.getElementById("loading-screen");const gallery=document.getElementById("gallery");setTimeout(()=>{loadingScreen.style.display="none";gallery.classList.remove("hidden");},1000);const options={threshold:0.1};const observer=new IntersectionObserver((entries,observerInstance)=>{entries.forEach((entry)=>{if(entry.isIntersecting){const img=entry.target;if(img.complete){img.classList.add("fade-in");}else{img.onload=()=>img.classList.add("fade-in");}
observerInstance.unobserve(img);}});},options);const images=document.querySelectorAll("#gallery img");images.forEach((img)=>observer.observe(img));});